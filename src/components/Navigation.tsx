'use client';

// Deprecated: As cascas de layout agora são fornecidas automaticamente por src/app/admin/layout.tsx e src/app/(funcionario)/layout.tsx
export function Navigation({ userRole }: { userRole?: 'EMPLOYEE' | 'ADMIN' }) {
  return null;
}
